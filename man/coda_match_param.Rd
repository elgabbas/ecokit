% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eco_coda_to_tibble.R
\name{coda_match_param}
\alias{coda_match_param}
\title{Retrieve a canonical parameter name from a HMSC coda object}
\usage{
coda_match_param(obj = NULL, param = NULL, warning = TRUE)
}
\arguments{
\item{obj}{A named list (typically a HMSC coda object produced by
\code{\link[Hmsc:convertToCodaObject]{Hmsc::convertToCodaObject()}}). Cannot be \code{NULL} or empty.}

\item{param}{Character. Parameter name to look up. Must be one of \code{"beta"},
\code{"alpha"}, \code{"omega"}, \code{"rho"}, or \code{"gamma"} (case-insensitive).}

\item{warning}{Logical. If \code{TRUE} (default), emit a warning when \code{param} is
not found in \code{obj}.}
}
\value{
The matched name as a character string, exactly as it appears in
\code{names(obj)}. Returns \code{NA_character_} when no match is found.
}
\description{
Performs a case-insensitive lookup of a requested HMSC posterior parameter
name against the names in \code{obj}. Returns the exact name as stored in the coda
object (e.g. \code{"AlphaInd"} when HMSC >= 3.4-0 is used, or \code{"Alpha"} for older
versions) so callers do not need to hard-code version-specific names.
}
\author{
Ahmed El-Gabbas
}
