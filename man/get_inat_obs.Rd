% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eco_inaturalist.R
\name{get_inat_obs}
\alias{get_inat_obs}
\title{Retrieve iNaturalist research-grade observations}
\usage{
get_inat_obs(
  taxon_id,
  bounds,
  year = NULL,
  month = NULL,
  max_results = 10000L,
  verbose = TRUE
)
}
\arguments{
\item{taxon_id}{\code{Integer}. iNaturalist taxon ID. Must be a positive integer
(e.g. \code{47158} for \emph{Animalia}). Find IDs at
\url{https://www.inaturalist.org/taxa}.}

\item{bounds}{\code{Numeric}. Geographic bounding box in WGS 84 decimal degrees,
specified as \code{c(sw_lat, sw_lng, ne_lat, ne_lng)}.}

\item{year}{\code{Integer} (optional). Four-digit calendar year to restrict the
query. \code{NULL} retrieves records for all years (use with caution for
widespread taxa --- chunking by month and then week is still applied, but
run time may be very long). Default: \code{NULL}.}

\item{month}{\code{Integer} (optional). Calendar month to restrict the query.
\code{NULL} iterates over all 12 months automatically. Ignored when \code{year = NULL}. Default: \code{NULL}.}

\item{max_results}{\code{Integer}. Maximum records per API window before the next
chunking level is triggered. The iNaturalist API enforces a hard ceiling of
10,000; increasing this value beyond 10,000 has no effect on the API but
will suppress intermediate splitting. Default: \code{10000L}.}

\item{verbose}{\code{Logical}. Whether to print progress messages to the console.
Default: \code{TRUE}.}
}
\value{
A deduplicated \code{tibble} with one row per research-grade observation
and the following columns:
\itemize{
\item \code{id}: \code{integer} --- unique iNaturalist observation ID
\item \code{observed_on}: \code{character} --- ISO 8601 observation date
\item \code{quality_grade}: \code{character} --- always \code{"research"} for
these queries
\item \code{taxon_id}: \code{integer} --- taxon ID of the observed organism
\item \code{taxon_name}: \code{character} --- scientific name
\item \code{latitude}: \code{numeric} --- decimal latitude (WGS 84)
\item \code{longitude}: \code{numeric} --- decimal longitude (WGS 84)
\item \code{place_guess}: \code{character} --- observer-provided locality
\item \code{user_login}: \code{character} --- iNaturalist username
\item \code{uri}: \code{character} --- permanent observation URL

An empty tibble (0 rows) is returned when no research-grade observations
exist for the query.
}
}
\description{
Downloads all research-grade observations from the \href{https://api.inaturalist.org/v1/docs/}{iNaturalist API} for a given taxon, spatial
bounding box, and optional year and/or month filter.
}
\details{
\subsection{Chunking cascade}{

The iNaturalist API silently caps every query at
\emph{10,000 records}. This function avoids silent truncation through an
automatic, multi-level chunking strategy:
\itemize{
\item Level 1: Annual window (year filter) --- if the annual count exceeds
\code{max_results}, split by month.
\item Level 2: Monthly window (month filter) --- if the monthly count exceeds
\code{max_results}, split by week.
\item Level 3: Weekly window (day range filter) --- if the weekly count exceeds
\code{max_results}, split by day.
\item Level 4: Daily window (day range filter) --- if the daily count exceeds
\code{max_results}, raise an error (spatial bounds too broad).
}

When \code{month} is supplied the cascade starts at level 2.
}

\subsection{Differences from \code{rinat::get_inat_obs()}}{
\itemize{
\item Uses \code{httr2} (retry, backoff, structured error handling) instead of
\code{httr}.
\item Automatically handles result sets larger than 10,000 records at any
temporal granularity, rather than requiring the user to supply chunked
queries manually.
\item Returns deduplicated results; iNaturalist's date-boundary indexing can
occasionally assign the same observation to two adjacent windows.
\item Raises a structured, informative error (with metadata) instead of
silently truncating when the record ceiling cannot be resolved.
\item Validates all inputs before making any network call.
}
}
}
\examples{
ecokit::load_packages(purrr)

# European bounding box
europe <- c(25, -30, 75, 50)

# All research-grade observations for a taxon in one month
obs_jan <- get_inat_obs(
  taxon_id = 67835, bounds = europe, year = 2024L, month = 1L)
obs_jan

# All months for one year (monthly / weekly / daily fallback automatic)
obs_2023 <- get_inat_obs(taxon_id = 67835, bounds = europe, year = 2023L)
obs_2023

# Multi-year loop with deduplication
obs_multi <- purrr::map_dfr(
  .x = 2020:2023,
  .f = ~ get_inat_obs(taxon_id = 67835, bounds = europe, year = .x))  \%>\%
  dplyr::distinct(id, .keep_all = TRUE)
obs_multi

}
\author{
Ahmed El-Gabbas
}
