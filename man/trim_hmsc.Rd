% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eco_trim_hmsc.R
\name{trim_hmsc}
\alias{trim_hmsc}
\title{Trim an \code{Hmsc} model object by removing specified components}
\usage{
trim_hmsc(model, names_to_remove = NULL)
}
\arguments{
\item{model}{An object of class \code{Hmsc}. Must not be \code{NULL}.}

\item{names_to_remove}{Character vector. Names of components to remove from
\code{model} (e.g. \code{c("postList", "rL", "ranLevels")}). Duplicate names are
silently dropped. All supplied names must exist in \code{model}; unrecognised
names trigger an error. Cannot be \code{NULL} or empty.}
}
\value{
An \code{Hmsc} object with the specified components removed. Returned
invisibly.
}
\description{
Removes named components from a fitted \code{\link[Hmsc:Hmsc]{Hmsc::Hmsc}} model object, returning
a lighter object that retains only the elements needed for downstream
computations. This is particularly useful when a fitted model is large
(several GB) and copies must be shipped to parallel workers via SLURM array
jobs or \code{future}-based parallel backends, where transferring the full object
would be prohibitively slow or exceed memory limits.
}
\details{
The function converts the \code{Hmsc} object to a plain \code{list} before
removing components, then restores the \code{"Hmsc"} class attribute. Conversion
to a plain list avoids S3-dispatch overhead that would otherwise be
triggered by modifying \code{Hmsc} slots directly.

Components are removed one at a time with a \code{for} loop rather than via
vectorised subsetting (e.g. \code{model[names_to_remove] <- NULL}). Although the
vectorised form is syntactically simpler, benchmarking on large models
inside SLURM-submitted jobs showed the iterative approach to be
substantially faster in practice, likely due to how R handles
copy-on-modify semantics for large named lists.

A call to \code{\link[base:gc]{base::gc()}} is made before returning to prompt immediate release
of the memory freed by removal.
}
\examples{
library(Hmsc)

# Fitted example model bundled with Hmsc
(model <- Hmsc::TD$m)

# Remove large posterior and random-level components
(trimmed_model <- trim_hmsc(
  model = model, names_to_remove = c("postList", "rL", "ranLevels")))

# Components that were removed
setdiff(names(model), names(trimmed_model))

# Memory savings
lobstr::obj_size(model)
lobstr::obj_size(trimmed_model)
}
\seealso{
\code{\link[Hmsc:Hmsc]{Hmsc::Hmsc()}} for the full model object structure;
\code{\link[Hmsc:sampleMcmc]{Hmsc::sampleMcmc()}} for the fitting step that populates \code{postList}.
}
\author{
Ahmed El-Gabbas
}
