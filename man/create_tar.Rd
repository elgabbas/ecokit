% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_create_tar.R
\name{create_tar}
\alias{create_tar}
\title{Create a tar archive from files in a directory}
\usage{
create_tar(
  files,
  dir_source,
  path_tar,
  compress = c("none", "gzip", "bzip2", "xz"),
  chmod = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{files}{Character vector of file names relative to \code{dir_source}. Must be
non-\code{NULL}, non-empty, and contain no \code{NA} values or absolute paths. All
named files must exist and be non-empty within \code{dir_source}.}

\item{dir_source}{Character scalar. Directory containing the files to
archive. The parent directory of \code{path_tar} is created if it does not
exist.}

\item{path_tar}{Character scalar. Path to the output tar archive.}

\item{compress}{Character scalar controlling compression. One of \code{"none"}
(default, plain \code{.tar}), \code{"gzip"} (\code{.tar.gz}), \code{"bzip2"} (\code{.tar.bz2}), or
\code{"xz"} (\code{.tar.xz}). The archive extension is not checked against this
choice; the caller is responsible for using a consistent file name.}

\item{chmod}{Character scalar. Optional Unix file permissions to set on the
created archive, as a three-digit octal string (e.g. \code{"644"}). If \code{NULL}
(the default) permissions are determined by the system. Values outside the
range \code{"000"}-\code{"777"} are rejected.}

\item{overwrite}{Logical scalar. Should an existing archive at \code{path_tar} be
overwritten? Defaults to \code{FALSE}.}
}
\value{
Invisibly returns \code{path_tar}.
}
\description{
Creates a tar archive (optionally compressed) containing the specified files
from a directory. The function uses the system \code{tar} executable and passes
the file list via a temporary file, avoiding command-line length limitations
when archiving large numbers of files.
}
\details{
The function requires a \code{tar} executable to be available on the
system \code{PATH}. File names in \code{files} must be relative to \code{dir_source};
absolute paths are not supported.

Compression support depends on the \code{tar} implementation and the presence of
the corresponding compressor (\code{gzip}, \code{bzip2}, or \code{xz}) on the system.
}
\examples{
ecokit::load_packages(fs, archive, purrr, dplyr)

# Create non-empty example files in a temporary directory
tmp <- fs::path_temp("example_files")
fs::dir_create(tmp)
files <- c("a.qs2", "b.qs2", "c.qs2")
purrr::walk(file.path(tmp, files), .f = ~ writeLines("test content", .x))

# Create an uncompressed tar archive
tar_file <- fs::path(tmp, "archive.tar")
create_tar(
  files = files, dir_source = tmp, path_tar = tar_file, overwrite = TRUE)

# Verify the created archive
archive::archive(tar_file)

ecokit::file_type(tar_file)

# Create a gzip-compressed archive
tar_gz_file <- fs::path(tmp, "archive.tar.gz")
create_tar(
  files = files, dir_source = tmp, path_tar = tar_gz_file,
  compress = "gzip", overwrite = TRUE)

# Compare the two archives
fs::file_info(c(tar_file, tar_gz_file)) \%>\%
 dplyr::select(path, size, type)

}
\author{
Ahmed El-Gabbas
}
