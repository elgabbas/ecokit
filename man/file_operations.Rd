% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_file_operations.R
\name{file_operations}
\alias{file_operations}
\alias{file_extension}
\alias{file_size}
\alias{file_type}
\title{File Information Utilities}
\usage{
file_extension(file)

file_size(file, ...)

file_type(file)
}
\arguments{
\item{file}{A character string representing the file path. Must not be \code{NULL}
and must be a character string. For \code{file_type}, the file must exist.}

\item{...}{Additional arguments passed to \link[gdata:humanReadable]{gdata::humanReadable} for
customizing the output format (for \code{file_size} only).}
}
\value{
\itemize{
\item \code{file_extension}: A character string with the file extension (e.g., "txt").
Returns an empty string if no extension is present.
\item \code{file_size}: A character string representing the file size in a
human-readable format (e.g., "1.2 MB").
\item \code{file_type}: A character string describing the file type (e.g., "ASCII
text").
}
}
\description{
A collection of functions to extract information about files, including their
extension, size, and type.
}
\details{
The following functions are included:
\itemize{
\item \code{file_extension}: Extracts the file extension from a file path using
\link[tools:fileutils]{tools::file_ext}. It does not verify file existence or content, only
parsing the extension from the file name.
\item \code{file_size}: Returns the file size in a human-readable format (e.g., KB,
MB, GB) using \link[gdata:humanReadable]{gdata::humanReadable}.
\item \code{file_type}: Determines the file type using the system's \code{file} command,
returning a description of the file type.
}
}
\note{
\itemize{
\item \code{file_extension} does not check file existence or content, only parsing the
extension from the file name.
\item \code{file_type} relies on the system's \code{file} command, so results may vary
across platforms.
}
}
\examples{
load_packages(terra)

f <- system.file("ex/elev.tif", package = "terra")

file_extension(f)                         # "tif"

file_size(f)                              # "7.8 KiB"

file_type(f)                              # e.g., "TIFF image data ..."
}
\author{
Ahmed El-Gabbas
}
