% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_validate_named_list.R
\name{validate_named_list}
\alias{validate_named_list}
\title{Validate a Named List Against Required Elements}
\usage{
validate_named_list(
  x,
  valid_names,
  object_name = deparse(substitute(x)),
  exact_length = TRUE
)
}
\arguments{
\item{x}{A named list to validate.}

\item{valid_names}{Character vector of required element names. Order is
ignored; validation uses set equality.}

\item{object_name}{Character string used in error messages to identify \code{x}.
Defaults to the unevaluated name of \code{x}.}

\item{exact_length}{Logical. If \code{TRUE} (default), \code{x} must contain exactly
\code{length(valid_names)} elements. If \code{FALSE}, only names are checked.}
}
\value{
Invisibly returns \code{TRUE} if all checks pass.
}
\description{
Checks that \code{x} is a named list whose elements match \code{valid_names}. A
contextual error is raised via \code{\link[=stop_ctx]{stop_ctx()}} if any check fails,
reporting missing or unexpected names where relevant.
}
\examples{
valid_names <- c("a", "b", "c")
validate_named_list(list(a = 1, b = 2, c = 3), valid_names)

\dontrun{
validate_named_list(list(a = 1, b = 2), valid_names)
validate_named_list(list(a = 1, b = 2, d = 3), valid_names)
}

}
\seealso{
\code{\link[=stop_ctx]{stop_ctx()}}
}
\author{
Ahmed El-Gabbas
}
