% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spat_set_raster_varnames.R
\name{set_raster_varnames}
\alias{set_raster_varnames}
\title{Set the variable name of a SpatRaster within a pipe}
\usage{
set_raster_varnames(rast, var_name)
}
\arguments{
\item{rast}{\verb{[SpatRaster]} A \code{SpatRaster} object whose variable name is to be
set. Must not be \code{NULL}.}

\item{var_name}{\verb{[character(1)]} or \code{NULL}. A single non-empty string to
assign as the variable name. If \code{NULL}, the variable name is reset to \code{""}
(terra's default empty state).}
}
\value{
The input \code{SpatRaster} with an updated \code{varnames} slot.
}
\description{
A small wrapper function that assigns a single variable name \code{varnames} to a
\code{SpatRaster} and returns the object, making it suitable for use in a
\code{magrittr} pipe without breaking the chain.
}
\section{\code{names} vs \code{varnames}}{
 \code{terra} stores two distinct name slots:
\itemize{
\item \strong{\code{names}} (per-layer) – used in plots, \code{as.data.frame()}, and
subsetting. Set via \code{names(x) <- ...} or \code{\link[terra:set.names]{terra::set.names()}}.
\item \strong{\code{varnames}} (per-data-source) – a single string inherited from the
source file variable name (e.g. NetCDF). For any in-memory or derived
\code{SpatRaster} there is always exactly one data source regardless of the
number of layers, so \code{varnames} only ever holds \strong{one} value.
}
}

\examples{
library(terra)

r <- terra::rast(
  ncols = 10, nrows = 10, nlyr = 3, vals = 1L, names = c("l1", "l2", "l3"))

# setting variable names directly with terra
terra::varnames(r) <- "v1"
r

# Set variable name within a pipe
(r2 <- set_raster_varnames(r, "temperature"))

# Reset variable name to empty
(r3 <- set_raster_varnames(r, NULL))
}
\author{
Ahmed El-Gabbas
}
