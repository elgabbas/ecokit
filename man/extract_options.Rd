% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_options.R
\name{extract_options}
\alias{extract_options}
\title{Extract Options Matching a Pattern}
\usage{
extract_options(pattern = "", case_sensitive = FALSE)
}
\arguments{
\item{pattern}{Character string. Pattern to search for in option names. If
empty (\code{""}), all options are returned.}

\item{case_sensitive}{Logical. If TRUE, pattern matching is case-sensitive.
Default is FALSE.}
}
\value{
A list of matched options, or all options if pattern is \code{""}. If no
matches are found, returns \code{NULL} (invisibly) and prints a message.
}
\description{
Returns a subset of the current R session options whose names match a given
pattern.
}
\examples{

# all options
all_options <- extract_options()
length(all_options)

# all options with "warn" in the name (case-insensitive)
extract_options(pattern = "warn")

# options starting with "r" (case-sensitive)
extract_options(pattern = "^r", case_sensitive = TRUE)

# non-existing pattern
extract_options(pattern = "non_existing_pattern_123")
}
\author{
Ahmed El-Gabbas
}
