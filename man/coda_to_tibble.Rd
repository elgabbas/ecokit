% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eco_coda_to_tibble.R
\name{coda_to_tibble}
\alias{coda_to_tibble}
\title{Convert an MCMC coda object to a tidy tibble}
\usage{
coda_to_tibble(coda_object = NULL, posterior_type = NULL, n_omega = 100L)
}
\arguments{
\item{coda_object}{An object of class \code{mcmc.list} or \code{mcmc}. Cannot be
\code{NULL}.}

\item{posterior_type}{Character string (case-insensitive). One of \code{"rho"},
\code{"alpha"}, \code{"beta"}, or \code{"omega"}. Determines the reshaping and
column-naming strategy applied to the raw MCMC matrix.}

\item{n_omega}{Positive integer. For \code{posterior_type = "omega"} only: the
number of species-pair columns to randomly sample from the coda object
before pivoting. If \code{n_omega} exceeds the number of available columns, all
columns are used and a warning is issued. Default: \code{100L}.}
}
\value{
A tibble whose structure depends on \code{posterior_type}; see Details.
}
\description{
Converts an object of class \code{mcmc.list} or \code{mcmc} — as produced by
\code{\link[coda:as.mcmc.list]{coda::as.mcmc.list()}} or \code{\link[Hmsc:convertToCodaObject]{Hmsc::convertToCodaObject()}} — into a tidy tibble.
Four posterior parameter types used in hierarchical models are supported:
\code{"rho"}, \code{"alpha"}, \code{"beta"}, and \code{"omega"}.
}
\details{
Each \code{posterior_type} produces a different tibble structure:
\itemize{
\item \strong{\code{"rho"}}: Returns a tibble with columns \code{chain}, \code{iter}, and \code{value}.
\item \strong{\code{"alpha"}}: Returns a tibble with columns \code{Alpha}, \code{alpha_num},
\code{factor}, \code{chain}, \code{iter}, and \code{value}. Column names in the coda object
must follow the pattern \verb{Alpha<N>[<num>, factor<k>]}.
\item \strong{\code{"beta"}}: Returns a nested tibble grouped by \code{variable} and \code{sp_id}
(species identifier). Coda dimnames must follow the Hmsc convention
\verb{B[<covariate>, <species_id>]}. Polynomial terms encoded as
\code{stats::poly(..., degree = 2, raw = TRUE)} are parsed and renamed with \verb{_l}
(linear) and \verb{_q} (quadratic) suffixes.
\item \strong{\code{"omega"}}: Samples \code{n_omega} species pairs at random and returns a
nested tibble grouped by \code{species_combs}, \code{sp1}, and \code{sp2}. Coda dimnames
must follow the Hmsc convention \verb{Omega1[<sp_id1>, <sp_id2>]}.
}
}
\examples{
#' # Example usage with a coda object from Hmsc::convertToCodaObject()
ecokit::load_packages(Hmsc, coda, dplyr)

coda_object <- Hmsc::convertToCodaObject(Hmsc::TD$m)

# ||||||||||||||||||||||||||||||||||||
# Alpha posterior samples
# ||||||||||||||||||||||||||||||||||||

dt_alpha <- coda_to_tibble(
   coda_object = coda_object$alpha[[1]], posterior_type = "Alpha")
dplyr::glimpse(dt_alpha)

# ||||||||||||||||||||||||||||||||||||
# Omega posterior samples
# ||||||||||||||||||||||||||||||||||||

dt_omega <- coda_to_tibble(
   coda_object = coda_object$Omega[[1]], posterior_type = "omega",
   n_omega = 10L)

dplyr::glimpse(dt_omega)

dplyr::glimpse(dt_omega$data[[1]])

# ||||||||||||||||||||||||||||||||||||
# Rho posterior samples
# ||||||||||||||||||||||||||||||||||||

dt_rho <- coda_to_tibble(
    coda_object = coda_object$rho, posterior_type = "rho")
dplyr::glimpse(dt_rho)

}
\seealso{
\code{\link[coda:as.mcmc.list]{coda::as.mcmc.list()}}, \code{\link[Hmsc:convertToCodaObject]{Hmsc::convertToCodaObject()}}
}
\author{
Ahmed El-Gabbas
}
