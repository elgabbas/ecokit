% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_validators.R
\name{is_integer}
\alias{is_integer}
\alias{validate_n_cores}
\alias{validate_strategy}
\alias{validate_slurm_runtime}
\alias{validate_slurm_ram}
\alias{validators}
\title{Argument validation helpers for ecokit}
\usage{
is_integer(x)

validate_n_cores(x)

validate_strategy(x)

validate_slurm_runtime(x, warning = TRUE)

validate_slurm_ram(x)
}
\arguments{
\item{x}{Input value
\itemize{
\item For \code{is_integer()} and \code{validate_n_cores()}, a numeric value to check.
\item For \code{validate_strategy()}, a character string naming a \code{future} strategy.
\item For \code{validate_slurm_runtime()} and \code{validate_slurm_ram()}, a character
string with a SLURM time or memory specification.
}}

\item{warning}{Logical, used only by \code{validate_slurm_runtime()}. If \code{TRUE}
(default), an invalid runtime string raises an error via
\link{stop_ctx}. If \code{FALSE}, an invalid runtime string instead raises a
\link[base:warning]{base::warning} and the function returns \code{FALSE}.}
}
\value{
\itemize{
\item \code{is_integer()}: a single logical (\code{TRUE}/\code{FALSE}) for numeric input;
errors if \code{x} is \code{NULL}.
\item \code{validate_n_cores()}: a single integer, clamped to
\code{\link[parallelly:availableCores]{parallelly::availableCores()}} if \code{x} exceeds it (with a warning).
\item \code{validate_strategy()}, \code{validate_slurm_ram()}: the unchanged input \code{x} if
valid; otherwise an error.
\item \code{validate_slurm_runtime()}: the unchanged input \code{x} if valid; otherwise an
error (\code{warning = TRUE}, default) or \code{FALSE} with a warning
(\code{warning = FALSE}).
}
}
\description{
A family of small, fast-failing validators used throughout \code{ecokit} (and
packages depending on it) to enforce argument types and formats —
particularly for HPC/SLURM job configuration (\code{mod_slurm()}) and parallel
processing setup (\code{set_parallel()}).
}
\details{
\itemize{
\item \code{is_integer()}: checks whether \code{x} is a single positive whole-number
value (e.g. \code{5}, \code{5L}, \code{5.0}). Returns \code{FALSE} for non-scalars, \code{NA}, zero,
negative, non-integer, or non-numeric values (without erroring), but throws
an error if \code{x} is \code{NULL}.
\item \code{validate_n_cores()}: validates a requested core count \code{x}. Must be a
positive integer scalar (checked via \code{is_integer()}; otherwise errors). If
\code{x} exceeds \code{\link[parallelly:availableCores]{parallelly::availableCores()}}, a warning is issued and the
number of available cores is used instead.
\item \code{validate_strategy()}: validates a \code{future} parallelisation strategy
name \code{x}. Must be one of \code{"sequential"}, \code{"multisession"}, \code{"multicore"}, or
\code{"cluster"}.
\item \code{validate_slurm_runtime()}: validates a SLURM \code{--time} string \code{x}.
Accepted formats follow SLURM conventions: \code{"minutes"}, \code{"minutes:seconds"},
\code{"hours:minutes:seconds"}, \code{"days-hours"}, \code{"days-hours:minutes"}, or
\code{"days-hours:minutes:seconds"}, with hours \code{00}-\code{23} and minutes/seconds
\code{00}-\code{59}.
\item \code{validate_slurm_ram()}: validates a SLURM \code{--mem} string \code{x}. Must be a
positive integer immediately followed by an uppercase unit: \code{K}, \code{M}, \code{G},
\code{T}, or their long forms \code{KB}, \code{MB}, \code{GB}, \code{TB}.
}
}
\examples{
# is_integer
is_integer(5)
is_integer(5L)
is_integer(5.5)
is_integer(-1)
is_integer(NA)
is_integer(c(1, 2))
is_integer("5")

\dontrun{
  is_integer(NULL) # Errors: `x` cannot be NULL
}


# validate_n_cores
validate_n_cores(2)

# Warns and is clamped to the number of available cores
validate_n_cores(1000L)

\dontrun{
  # Errors: not a positive integer scalar
  validate_n_cores(-1)
  validate_n_cores("2")
}


# validate_strategy
validate_strategy("multisession")
\dontrun{
  validate_strategy("parallel") # Errors: not a valid `future` strategy
}

# validate_slurm_runtime ----
validate_slurm_runtime("30")
validate_slurm_runtime("2:00:00")
validate_slurm_runtime("0-12:00:00")
validate_slurm_runtime("5-23:59")
validate_slurm_runtime("2:00")

\dontrun{
  validate_slurm_runtime("2:70:00")     # Errors: invalid minutes
  validate_slurm_runtime("12-24:00:00") # Errors: invalid hours
  validate_slurm_runtime("abc")         # Errors: not a SLURM time format
}

# Returns FALSE with a warning instead of erroring
validate_slurm_runtime("abc", warning = FALSE)


# validate_slurm_ram ----
validate_slurm_ram("8G")
validate_slurm_ram("1024M")
validate_slurm_ram("32GB")
validate_slurm_ram("1T")

\dontrun{
  validate_slurm_ram("500")    # Errors: missing unit
  validate_slurm_ram("500m")   # Errors: lowercase unit not accepted
  validate_slurm_ram("4.5G")   # Errors: non-integer amount
  validate_slurm_ram("100MBs") # Errors: trailing characters
  validate_slurm_ram("GB32")   # Errors: unit before amount
}
}
\author{
Ahmed El-Gabbas
}
