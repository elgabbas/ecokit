% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spat_sort_raster_layers.R
\name{sort_raster_layers}
\alias{sort_raster_layers}
\title{Sort raster layers by name using natural ordering}
\usage{
sort_raster_layers(r)
}
\arguments{
\item{r}{A \code{terra::SpatRaster}.}
}
\value{
A \code{terra::SpatRaster} with layers reordered according to natural
sorting of layer names.
}
\description{
Sorts the layers of a \code{terra::SpatRaster} according to their layer names
using natural (mixed) ordering. Numeric components of layer names are ordered
numerically rather than lexicographically (e.g. \code{"layer2"} is placed before
\code{"layer10"}).
}
\details{
If duplicate layer names are detected, a warning is issued because the
resulting order of layers with identical names cannot be distinguished by
name alone. Layer order is preserved among duplicated names according to
their original positions.
}
\examples{
ecokit::load_packages(terra, dplyr, dismo)

fnames <- list.files(
  path = paste(system.file(package = "dismo"), "/ex", sep = ""),
  pattern = "grd", full.names = TRUE)
(r <- terra::rast(fnames))
names(r)

(r2 <- sort_raster_layers(r))
names(r2)

# ---------------------------------------------------

r <- terra::rast(nrows = 10, ncols = 10, nlyrs = 4, vals = 1) \%>\%
   stats::setNames(c("layer10", "layer2", "layer10", "layer1"))
r

sort_raster_layers(r)
}
\author{
Ahmed El-Gabbas
}
