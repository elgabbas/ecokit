% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_cat_names.R
\name{cat_names}
\alias{cat_names}
\title{Print Names of an Object with Optional Sorting}
\usage{
cat_names(x, sort = FALSE, sep = "\\n", na_rm = TRUE, prefix = NULL, ...)
}
\arguments{
\item{x}{An R object with names attribute (e.g., vector, list, data frame).}

\item{sort}{Logical. Whether to sort names using mixed alphanumeric sorting.
Defaults to \code{FALSE}.}

\item{sep}{Character. Separate names in output. Defaults to newline (\code{"\\n"}).}

\item{na_rm}{Logical. Whether to removes \code{NA} values from names before
printing. Defaults to \code{TRUE}.}

\item{prefix}{Character. string to prepend to each name. Defaults to \code{NULL}.}

\item{...}{Additional arguments passed to \code{\link[base:cat]{base::cat()}}.}
}
\value{
Invisibly returns the vector of names (sorted if \code{sort = TRUE}).
}
\description{
Prints the names of an object to the console, with an option to sort them.
Supports custom separators and handles various edge cases.
}
\examples{
# Basic usage
vec <- c(a1 = 1, b = 2, a2 = 3)
cat_names(vec)

# Sorted names
cat_names(vec, sort = TRUE)

# Custom separator and prefix
cat_names(vec, sep = ", ", prefix = "- ")

# Handle NA names
vec_na <- c(a = 1, NA, c = 3)
cat_names(vec_na, na_rm = TRUE)
cat_names(vec_na, na_rm = TRUE, sort = TRUE)

# Example with data frames
cat_names(mtcars)
cat_names(iris)
}
\author{
Ahmed El-Gabbas
}
