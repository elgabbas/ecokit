% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_find_capital_names.R
\name{find_capital_names}
\alias{find_capital_names}
\title{Find files and directories with uppercase letters in their names}
\usage{
find_capital_names(
  path = ".",
  extension = NULL,
  exclude_extension = NULL,
  exclude_dirs = c(".git", ".Rproj.user"),
  full_paths = FALSE
)
}
\arguments{
\item{path}{Character scalar. Root directory to search recursively.}

\item{extension}{Optional character vector of file extensions to include
(case-insensitive). If \code{NULL} (default), all file types are searched.
Extensions may be supplied with or without a leading \code{"."}.}

\item{exclude_extension}{Optional character vector of file extensions to
exclude. Matching is case-insensitive.}

\item{exclude_dirs}{Character vector of directory names to exclude together
with all of their contents.}

\item{full_paths}{Logical scalar. If \code{TRUE}, absolute paths are returned.
Otherwise, paths are returned relative to \code{path} (default).}
}
\value{
A named list with elements:
\itemize{
\item \code{files}: Files whose filenames contain uppercase letters.
\item \code{directories}: Directories whose own names contain uppercase letters.
\item \code{directories_anywhere_in_path}: Directories whose relative paths contain
uppercase letters anywhere in the path hierarchy.
}
}
\description{
Recursively searches a directory tree and reports files and directories whose
names contain one or more uppercase letters. This is useful for enforcing
project-wide naming conventions that require lowercase file and directory
names, especially when working on case-sensitive file systems such as Linux
or HPC environments.
}
\details{
By default, paths are returned relative to the search root. Absolute paths
can be returned by setting \code{full_paths = TRUE}.

This function is useful for identifying violations of lowercase
naming conventions in projects. Detecting uppercase letters early helps
avoid problems caused by case-sensitive file systems and promotes
consistent project structure.
}
\examples{
\dontrun{
  # Search entire working directory ---
  find_capital_names()

  # Restrict to specific file extensions ---
  find_capital_names(extension = c("R", "Rmd", "RData")

  # Exclude file extensions ---
  find_capital_names(exclude_extension = c("RData", "qs2", "feather"))

  # Exclude directories ---
  find_capital_names(
      exclude_dirs = c(".git", ".Rproj.user", ".vscode", "renv"))

  # Return absolute paths ---
  find_capital_names(full_paths = TRUE)
}
}
\author{
Ahmed El-Gabbas
}
